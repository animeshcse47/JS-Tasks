# Fake Store React

Simple Vite + React app that fetches products from the Fake Store API and displays them.

Steps to run:

```bash
cd "d:\coding\PLACEMENT TRAINING\fake-store-react"
npm install
npm run dev
```

Then open the local dev URL (usually http://localhost:5173).
